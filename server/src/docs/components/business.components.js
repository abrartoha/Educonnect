export const businessSchemas = {
  Campaign: {
    type: 'object',
    properties: {
      id: { type: 'string' },
      name: { type: 'string' },
      audience: { type: 'string' },
      startDate: { type: 'string', format: 'date-time' },
      endDate: { type: 'string', format: 'date-time' },
      status: { type: 'string', enum: ['DRAFT', 'ACTIVE', 'PAUSED', 'ENDED'] },
      createdAt: { type: 'string', format: 'date-time' },
      updatedAt: { type: 'string', format: 'date-time' },
    },
  },
  CreateCampaign: {
    type: 'object',
    required: ['name', 'audience', 'startDate', 'endDate'],
    properties: {
      name: { type: 'string', minLength: 3, maxLength: 200 },
      audience: { type: 'string', minLength: 2, maxLength: 200 },
      startDate: { type: 'string', format: 'date-time' },
      endDate: { type: 'string', format: 'date-time' },
      status: { type: 'string', enum: ['DRAFT', 'ACTIVE', 'PAUSED', 'ENDED'], default: 'DRAFT' },
    },
  },
  UpdateCampaign: {
    type: 'object',
    properties: {
      name: { type: 'string', minLength: 3, maxLength: 200 },
      audience: { type: 'string', minLength: 2, maxLength: 200 },
      startDate: { type: 'string', format: 'date-time' },
      endDate: { type: 'string', format: 'date-time' },
      status: { type: 'string', enum: ['DRAFT', 'ACTIVE', 'PAUSED', 'ENDED'] },
    },
  },
  Lead: {
    type: 'object',
    properties: {
      id: { type: 'string' },
      studentId: { type: 'string' },
      targetId: { type: 'string' },
      targetRole: { type: 'string', enum: ['UNIVERSITY', 'AGENT', 'CONSULTANT'] },
      programme: { type: 'string', nullable: true },
      message: { type: 'string' },
      status: { type: 'string', enum: ['NEW', 'CONTACTED', 'CONVERTED', 'CLOSED'] },
      createdAt: { type: 'string', format: 'date-time' },
      updatedAt: { type: 'string', format: 'date-time' },
    },
  },
  LeadWithStudent: {
    type: 'object',
    properties: {
      id: { type: 'string' },
      studentId: { type: 'string' },
      targetId: { type: 'string' },
      targetRole: { type: 'string', enum: ['UNIVERSITY', 'AGENT', 'CONSULTANT'] },
      programme: { type: 'string', nullable: true },
      message: { type: 'string' },
      status: { type: 'string', enum: ['NEW', 'CONTACTED', 'CONVERTED', 'CLOSED'] },
      createdAt: { type: 'string', format: 'date-time' },
      updatedAt: { type: 'string', format: 'date-time' },
      student: {
        type: 'object',
        properties: {
          id: { type: 'string' },
          name: { type: 'string' },
          avatarUrl: { type: 'string', nullable: true },
          email: { type: 'string' },
          role: { type: 'string' },
        },
      },
    },
  },
  LeadWithTarget: {
    type: 'object',
    properties: {
      id: { type: 'string' },
      studentId: { type: 'string' },
      targetId: { type: 'string' },
      targetRole: { type: 'string', enum: ['UNIVERSITY', 'AGENT', 'CONSULTANT'] },
      programme: { type: 'string', nullable: true },
      message: { type: 'string' },
      status: { type: 'string', enum: ['NEW', 'CONTACTED', 'CONVERTED', 'CLOSED'] },
      createdAt: { type: 'string', format: 'date-time' },
      updatedAt: { type: 'string', format: 'date-time' },
      target: {
        type: 'object',
        properties: {
          id: { type: 'string' },
          name: { type: 'string' },
          avatarUrl: { type: 'string', nullable: true },
          email: { type: 'string' },
          role: { type: 'string' },
        },
      },
    },
  },
  CreateLead: {
    type: 'object',
    required: ['targetId', 'message'],
    properties: {
      targetId: { type: 'string', description: 'CUID of the university, agent, or consultant' },
      programme: { type: 'string', maxLength: 200 },
      message: { type: 'string', minLength: 10, maxLength: 2000 },
    },
  },
  UpdateLeadStatus: {
    type: 'object',
    required: ['status'],
    properties: {
      status: { type: 'string', enum: ['NEW', 'CONTACTED', 'CONVERTED', 'CLOSED'] },
    },
  },
  Review: {
    type: 'object',
    properties: {
      id: { type: 'string' },
      reviewerId: { type: 'string' },
      targetId: { type: 'string' },
      targetRole: { type: 'string', enum: ['UNIVERSITY', 'AGENT', 'CONSULTANT'] },
      rating: { type: 'integer', minimum: 1, maximum: 5 },
      title: { type: 'string', nullable: true },
      body: { type: 'string' },
      verified: { type: 'boolean' },
      createdAt: { type: 'string', format: 'date-time' },
    },
  },
  ReviewWithReviewer: {
    type: 'object',
    properties: {
      id: { type: 'string' },
      reviewerId: { type: 'string' },
      targetId: { type: 'string' },
      targetRole: { type: 'string', enum: ['UNIVERSITY', 'AGENT', 'CONSULTANT'] },
      rating: { type: 'integer', minimum: 1, maximum: 5 },
      title: { type: 'string', nullable: true },
      body: { type: 'string' },
      verified: { type: 'boolean' },
      createdAt: { type: 'string', format: 'date-time' },
      reviewer: {
        type: 'object',
        properties: {
          id: { type: 'string' },
          name: { type: 'string' },
          avatarUrl: { type: 'string', nullable: true },
          role: { type: 'string' },
        },
      },
    },
  },
  CreateReview: {
    type: 'object',
    required: ['targetId', 'rating', 'body'],
    properties: {
      targetId: { type: 'string', description: 'CUID of the university, agent, or consultant' },
      rating: { type: 'integer', minimum: 1, maximum: 5 },
      title: { type: 'string', maxLength: 120 },
      body: { type: 'string', minLength: 5, maxLength: 2000 },
    },
  },
};
